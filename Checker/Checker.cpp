#include <cstdio>

#include "..\Common\FileIO.h"

class THashCheker
{
public:
	THashCheker(const std::string& filename)
	{
		
	}
};

int main()
{
	TLineReader fInput("..\\surls");
	char* buffer;
	size_t urlLen;
	while (fInput)
	return 0;
}